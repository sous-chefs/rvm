# RVM doesn't have a script resource - use execute with RVM environment instead
# This recipe is kept for reference but the rbenv_script resource doesn't apply to RVM

# Example of running a command with RVM:
# execute 'run_rake' do
#   command 'bash -l -c "source /usr/local/rvm/scripts/rvm && rvm default do rake nadda"'
# end
